#include "push_swap.h"

/*int    *list_on_tab(pile **a)
{
    int *str;
    int i;
    pile *tmp;

    i = 0;
    str = malloc(sizeof(int ) * ft_strlenlist(a) + 1);
    tmp = *a;
    while ((tmp)->next)
    {
        str[i] = (tmp)->content;
        i++;
        tmp = tmp->next;
    }
    if (tmp->next == NULL && tmp->content)
        str[i] = (tmp)->content;
    return (str);
}*/